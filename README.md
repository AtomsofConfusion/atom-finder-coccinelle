# Using the Tool
By default, the cocci_outputs folder is empty, and one will first run
> atom-finder-coccinelle

to obtain the output files.

Then to perform comparsions, run
> atom-finder-coccinelle-tests
